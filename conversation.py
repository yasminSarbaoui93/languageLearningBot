# File to manage the conversation handler of the chatbot

#will use direcrtly LLM in openAI

#checking pricing / possible to use Mini model

# All API requests should include your API key in an Authorization HTTP header as follows: Authorization: Bearer OPENAI_API_KEY

from openai import OpenAI
